{
    "name": "demvsystems/smtp-validator",
    "description": "SMTP-Anmeldedatenvalidierung",
    "type": "project",
    "authors": [
        {
            "name": "Demvsystems",
            "email": "admin@demv.de"
        }
    ],
    "require-dev": {
        "phpunit/phpunit": "^7.1"
    },
    "autoload": {
        "psr-4": {
            "Demv\\SmtpValidator\\": "src/"
        }
    },
    "require": {
        "ext-json": "*",
        "ext-sockets": "*",
        "symfony/serializer": "^4.0",
        "dgame/php-ensurance": "^2.0",
        "netresearch/jsonmapper": "^1.4",
        "ext-openssl": "*"
    },
    "scripts": {
        "test": [
            "vendor/bin/phpunit"
        ],
        "coverage": [
            "vendor/bin/phpunit --coverage-clover=coverage"
        ]
    }
}
